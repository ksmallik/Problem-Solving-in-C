#include <stdio.h>
#include <math.h>

int main() {
    // int x = 5;
    // float y = 0;
    // printf("typeof(x / 2.0)");

    // int a, b = 10;
    // a = ++b + --b;
    // printf("a = %d\n", a);

    float x = 24.0;
    printf("%f %f",  x++, sqrt(x));



    return 0;
}
